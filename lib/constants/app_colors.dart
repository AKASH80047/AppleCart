import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ================= PRIMARY =================

  static const Color primary = Color(0xFF2878E5);
  static const Color primaryDark = Color(0xFF1E63C7);
  static const Color primaryLight = Color(0xFFEAF3FF);

  // ================= BACKGROUND =================

  static const Color background = Color(0xFFF8F9FD);
  static const Color scaffold = Color(0xFFF8F9FD);
  static const Color white = Colors.white;

  // ================= TEXT =================

  static const Color textPrimary = Color(0xFF171717);
  static const Color textSecondary = Color(0xFF757575);
  static const Color hint = Color(0xFFBDBDBD);

  // ================= BORDER =================

  static const Color border = Color(0xFFE5E5E5);
  static const Color divider = Color(0xFFF1F1F1);

  // ================= STATUS =================

  static const Color success = Color(0xFF22C55E);
  static const Color warning = Color(0xFFFFB547);
  static const Color error = Color(0xFFE53935);

  // ================= PRICE =================

  static const Color price = primary;
  static const Color oldPrice = Color(0xFF9E9E9E);

  // ================= PRODUCT COLORS =================

  static const Color desertTitanium = Color(0xFFB79F8A);
  static const Color naturalTitanium = Color(0xFFD8D3CC);
  static const Color whiteTitanium = Color(0xFFF5F5F2);
  static const Color blackTitanium = Color(0xFF3C3C3D);

  static const Color silver = Color(0xFFE2E2E2);
  static const Color orange = Color(0xFFFF5A2B);
  static const Color black = Color(0xFF1A1A1A);

  // ================= CARD =================

  static const Color card = Colors.white;
  static const Color cardBackground = Color(0xFFF5F5F5);

  // ================= ICON =================

  static const Color icon = Color(0xFF5F6368);

  // ================= SHADOW =================

  static const Color shadow = Color(0x14000000);
}
